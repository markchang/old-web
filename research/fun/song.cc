// name that song
// mark l. chang
// written while waiting for something to compile

#include "fred.h"

// prototypes for this file
void intro();
void verse(int);
void chorus(int);
void bridge();
void end();

int main()
{
  intro();
  for( int v=1; v<3; v++ ) {
	 verse(v);
	 chorus(v);
  }
  bridge();

  verse(3); verse(4);
  end();
}

void intro()
{
  This = real_life   ? yes : no;
  This = (is)fantasy ? yes : no;

  caught_in( "a landslide" );

  while(1) {
	 int from = reality();
	 if( from == yes )
		break; // escape
  }

  FILE *f = fopen("your_eyes","r");
  char the_skies = 'h';
  
  l[0][0].k = (up)the_skies;
  and_see(l[0][0].k);

  int me = poor_boy;  // need no sympathy

  switch (me) {
  case easy_come:
	 wind_blows(me);
	 break;
  case easy_go:
	 wind_blows(me);
	 break;
  case little_high:
	 wind_blows(me);
	 break;
  case little_low:
	 wind_blows(me);
	 break;
  default:
	 wind_blows(me); // doesn't really matter
  }
  
  2*me;
}

void verse(int v)
{
  int man,gun,head,trigger,dead,life=1;
  char *Im, *Hes;

  switch (v) {
  case 1:
	 // verse 1
	 begin(life);
 	 assert(mama);
	 man=0;
 	 kill(man,man);
 	 gun = yes; head = yes;	 trigger = 9*yes;
	 dead = gun*head + pull(trigger);
	 if( life ) 
		kill(life,man);
	 else
		kill(man,man);
	 break;
  case 2:
	 // verse 2
	 // 	 too late, my time has come,
	 // 	 sends shivers down my spine
	 //     body's aching all the time,
	 //     Goodbye everybody-I've got to go
	 //     gotta leave you all behind and face the truth
	 break;
  case 3:
	 // verse 3
	 Im = "just a poor boy";
	 Im = "Nobody loves me";
	 Hes = "just a poor boy";
	 Hes = "from a poor family";

	 for( int i=0; i<5; i++ ) {
		char Bismillah[50];
		char backup_vocals[50];
		sprintf(Bismillah,"%s","We will not let you go");
		sprintf(backup_vocals,"%s","Let him go!");
	 }

	 no & no & no & no & no & no & no;
	 int mama_mia;
	 for( int let_me_go=0; let_me_go<3; let_me_go++ ) {
		mama_mia = let_me_go;
	 }
	 
	 // set aside devil
	 break;
  case 4:
	 // verse 4
	 so_you_think("you can stone me and spit in my eye",0);
	 so_you_think("you can love me and leave me to die",1);
	 int baby;
	 baby = !This; // to me
	 just_gotta("get out");
	 just_gotta("get right oudda here");
	 break;
  default:
	 break;
  }

}
	 
void chorus(int v)
{
  switch(v) {
  case 1:
	 // chorus 1
	 int back,time,tomorrow,cry;
	 char* carry_on;

	 mama = 00000000000;
	 cry=1;
	 do {
		back=0;
		time=yes;
		tomorrow=yes;
		carry_on = "nothing really matters";
	 } while(!cry);
	 if( !back && time==tomorrow) {
		carry_on && carry_on;
		as_if( carry_on ); // see def
	 }
	 break;
  case 2:
	 // chorus 2
	 int die, born;
	 mama = 00000000000000000000; // any way the wind blows
	 !die && !die && !die;
	 !born && !born && !born;
	 break;
  }
}

void bridge()
{
  ic("a silhouett-o of a man");
  scaramouche('h',0);
  scaramouche('a',1);
  do {
	 fandango();
  } while(0);

  int very_frightening = thunderbolt && lightning;

  galileo & galileo & galileo & galileo;
  galileo & figaro == magnifico;
}

void end()
{
  int really_matters=-1;

  switch (really_matters) {
  case 0:
	 // blah
	 break;
  case 1:
	 // blah
	 break;
  case 2:
	 // blah
	 break;
  default:
	 // nothing
	 break;
  }

  char anyone_can_see[50];
  sprintf(anyone_can_see,"%s","Nothing really matters");
  strcat(anyone_can_see," - to me.");
  done();
}
